export function linkBtnStyle(color: string): React.CSSProperties {
    return {
        background: "none", border: "none", padding: 0, color,
        fontSize: 12, cursor: "pointer", fontFamily: "inherit", textDecoration: "underline",
    };
}

